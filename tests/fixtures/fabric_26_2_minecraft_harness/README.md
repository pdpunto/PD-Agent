# Fabric 26.2 bounded harness fixture

This fixture is the M3 26.2 profile boundary. It reuses the observation
implementation from the validated L11 harness and deliberately declares the
unobfuscated 26.2 toolchain without Yarn mappings.

Live compilation and runtime validation belong to LOT 10.
