[build-system]
requires = []
build-backend = "pd_agent_build_backend"
backend-path = ["."]

[project]
name = "pd-agent"
version = "0.1.0"
description = "PD Agent foundation package."
readme = "README.md"
requires-python = ">=3.13"
license = {text = "Proprietary"}

[project.scripts]
pd-agent = "pd_agent.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

